[project]
name = "notes-api"
version = "0.1.0"
description = "Notes API — Proje 03 starter (sureklilik artefakti yok)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
