[project]
name = "notes-api"
version = "0.1.0"
description = "Notes API — Proje 04 starter (free-form features.md)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
