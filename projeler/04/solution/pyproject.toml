[project]
name = "notes-api"
version = "0.1.0"
description = "Notes API — Proje 04 solution (features.json + verifier + WIP=1)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
