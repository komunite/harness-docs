[project]
name = "notes-api"
version = "0.1.0"
description = "Notes API — Proje 05 solution (executor + verifier ayrimi, uc katmanli kapi)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
